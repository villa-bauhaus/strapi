'use strict';

/**
 * newletter-subscription router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::newletter-subscription.newletter-subscription');
