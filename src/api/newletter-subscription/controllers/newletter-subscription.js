'use strict';

/**
 * newletter-subscription controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::newletter-subscription.newletter-subscription');
